{%- if ci_platform != "none" -%}
name: Typos

on:
  push:
    branches: [main]
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
{% include "snippets/github/jobs/spellcheck.yml" %}
{%- endif %}
