[placeholders.ci_platform]
type = "select"
prompt = "CI platform"
options = ["github", "none"]
default = "github"

[placeholders.post_issue]
type = "bool"
prompt = "Publish RustSec audit issues on scheduled runs?"
default = "true"

[[hints]]
message = "Edit .cargo/audit.toml for severity thresholds and advisory ignores."
