{%- if ci_platform != "none" -%}
name: Benchmarks

on:
  push:
    branches: [main]
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
{% include "snippets/github/jobs/bench.yml" %}
{%- endif %}
