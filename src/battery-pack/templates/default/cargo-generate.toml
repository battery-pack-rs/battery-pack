[template]
cargo_generate_version = ">=0.22.0"
ignore = ["hooks"]

[hooks]
pre = ["hooks/pre-script.rhai"]

[placeholders]
# No additional placeholders - cargo-generate provides project-name, crate_name, authors
